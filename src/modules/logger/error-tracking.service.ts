export class ErrorTrackingService {
  warn(message: string, context: string) {
    // send warn to error tracking platform
  }

  error(message: string, context: string) {
    // send error to error tracking platform
  }
}
